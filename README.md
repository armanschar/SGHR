C#.NET Razor Pages App, que crea un API, y lo consume mediante WebApp, utiliza SOC, entity framework, mapea entidades de una base de datos, contiene Unit Test tanto para repositorio como servicio, y trabaja con una estructura en capas.

La rama Reservation es la que me pertenece y en la única que trabajé.
